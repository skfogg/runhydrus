.onLoad <- function(libname, pkgname) {
  suppressPackageStartupMessages({
    library(crayon)  # Example: load without startup messages
  })
  suppressWarnings({library(crayon)} )
}
.onAttach <- function(libname, pkgname) {
  suppressPackageStartupMessages({
    library(crayon)  # Example: load without startup messages
  })
  suppressWarnings({library(crayon)} )
}
